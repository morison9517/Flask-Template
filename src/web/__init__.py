# =============================================================================
# 「このフォルダは web という1つのまとまり(パッケージ)です」という表札。
#
#   これがあるので、他のファイルから
#       from web.models import User
#   と呼び出せる。
#
#   ★中身は空のままでOK。アプリを組み立てる本体は app.py にある。
# =============================================================================
